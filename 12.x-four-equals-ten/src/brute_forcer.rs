use crate::evaluator;



pub fn brute_force(input: &Vec<u8>) -> String {
	// evaluator::evaluate("(3*5)/7+0-1*2*9/(8+4)*6".to_string());
	evaluator::evaluate("9*(1/9+1)".to_string());

	return String::from("");
}
